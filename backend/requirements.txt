fastapi==0.115.8
uvicorn[standard]==0.30.6
pydantic==2.10.6
pydantic-settings==2.7.1
python-multipart==0.0.9
httpx==0.27.2

# ML/AI dependencies (optional, for ML detectors)
torch>=2.0.0; platform_system != "Windows" or python_version < "3.14"
transformers>=4.30.0; platform_system != "Windows" or python_version < "3.14"

# Testing
pytest>=7.4.0
pytest-asyncio>=0.21.0

# NOTE:
# ML dependencies (torch/transformers/sklearn/numpy/etc.) are intentionally NOT required
# for the initial prototype API, to keep installs smooth on newer Python versions (e.g., 3.14)
# and on machines without native build toolchains.


